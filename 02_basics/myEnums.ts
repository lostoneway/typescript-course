//used to restrict user choice! 

const enum SeatChoice {
    aisle, //you could say AISLE = 10. So the follow enums would be 11, 12 ,13 
    middle,
    window
}

const mySeat = SeatChoice.aisle

export{}